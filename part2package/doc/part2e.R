## ----include = FALSE----------------------------------------------------------
#| label: setup

# For development: loads package from source
if (requireNamespace("devtools", quietly = TRUE)) {
  devtools::load_all()
}

# Load dependencies
library(ggplot2)

